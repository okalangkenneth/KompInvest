<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - KOMPINVEST</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="@Url.Action("Index", "Home")"><img src="~/images/LogoMakr-45B09I.png" alt="Logo" class="logo"> <strong>KOMPISAR INVESTMENT CLUB</strong></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active"><a class="nav-link" href="@Url.Action("Index", "Home")">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("About", "Home")">About Us</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("News", "Home")">Blog/News</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Events", "Home")">Events</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Resources", "Home")">Resources</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Contact", "Home")">Contact Us</a></li>
                <li class="nav-item"><a class="nav-link" href="javascript:openLoginModal()">Member Login</a></li>
                @* <li class="nav-item"><a class="nav-link" href="javascript:openRegisterModal()">Register</a></li> *@
                <!-- Correct placement for the Learn More button -->
                <li class="nav-item">
                    <a href="@Url.Action("LearnMore", "Home")" class="learn-more-btn">Learn More</a>
                </li>
            </ul>

            <ul class="navbar-nav ml-auto social-icons">
                <li class="nav-item"><a href="#" class="nav-link facebook-icon"><i class="fab fa-facebook-f"></i></a></li>
                <li class="nav-item"><a href="#" class="nav-link instagram-icon"><i class="fab fa-instagram"></i></a></li>
                <li class="nav-item"><a href="#" class="nav-link linkedin-icon"><i class="fab fa-linkedin-in"></i></a></li>
            </ul>
        </div>
    </nav>


    <!-- Banner Section -->
    <header class="header-content">
        <h1 class="text-white">Investing Together, Growing Together</h1>
    </header>


    <main role="main" class="pb-3">
        @RenderBody()
    </main>

    <!-- Register Modal -->
    <div class="modal fade" id="registerModal" tabindex="-1" role="dialog" aria-labelledby="registerModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="registerModalLabel">Register</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="registerModalContent">
                    <!-- Content will be loaded here -->
                </div>
            </div>
        </div>
    </div>

    <!-- Login Modal -->
    <div class="modal fade" id="loginModal" tabindex="-1" role="dialog" aria-labelledby="loginModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="loginModalLabel">Login</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form id="loginForm" novalidate>
                        <div class="form-group">
                            <label for="email">Email address</label>
                            <input type="email" class="form-control" id="email" placeholder="Enter email" required>
                            <div class="invalid-feedback">Please enter a valid email.</div>
                        </div>
                        <div class="form-group">
                            <label for="password">Password</label>
                            <input type="password" class="form-control" id="password" placeholder="Password" required>
                            <div class="invalid-feedback">Please enter your password.</div>
                        </div>
                        <button type="submit" class="btn btn-primary">Login</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Chatbot UI -->
    <div class="chatbot-ui" id="chatbotUI" onclick="toggleChatbot()">
        <img src="/images/LogoMakr-1ObF0a.png" alt="Chat with us" class="chatbot-icon">
        <div class="chatbot-body p-3">
            <!-- Chat content goes here -->
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        if (typeof jQuery == 'undefined') {
            document.write(unescape("%3Cscript src='/path/to/local/jquery-3.7.1.min.js' type='text/javascript'%3E%3C/script%3E"));
        }
    </script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.5/jquery.validate.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/4.0.0/jquery.validate.unobtrusive.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            // Expose your functions to the global scope if needed
            window.toggleChatbot = toggleChatbot;
            window.openLoginModal = openLoginModal;
            window.openRegisterModal = openRegisterModal;

            // Gradual fade-in effect for the background image
            $('body').hide().fadeIn(4000);

            // Toggle chatbot UI
            function toggleChatbot() {
                $('#chatbotUI').toggle();
            }

            // Open login modal
            function openLoginModal() {
                console.log("Opening Login Modal...");
                loadModalContent('/Account/Login', '#loginModal', '#loginModal .modal-body');
            }

            // Open register modal
            function openRegisterModal() {
                console.log("Opening Register Modal...");
                loadModalContent('/Account/Register', '#registerModal', '#registerModalContent');
            }

            // Load modal content
            function loadModalContent(url, modalId, contentId) {
                $(contentId).load(url, function (response, status, xhr) {
                    if (status === "error") {
                        console.error(`Error loading modal from ${url}: ${xhr.status} ${xhr.statusText}`);
                    }
                    $(modalId).modal('show');
                });
            }

            // Extract form validation to a separate function
            function validateForm(formElement) {
                var isValid = formElement.checkValidity();
                if (!isValid) {
                    event.stopPropagation();
                }
                formElement.classList.add('was-validated');
                return isValid;
            }

            // Extract AJAX request to a separate function
            function performAjaxRequest(formElement, successCallback) {
                console.log("Performing AJAX request...");
                $.ajax({
                    url: $(formElement).attr('action'),
                    type: 'POST',
                    data: $(formElement).serialize(),
                    success: successCallback,
                    error: function (xhr, status, error) {
                        alert('An error occurred: ' + xhr.status + ' ' + xhr.statusText);
                    }
                });
            }

            // AJAX form submission for login
            $('#loginForm').on('submit', function (event) {
                event.preventDefault();
                if (validateForm(this)) {
                    performAjaxRequest(this, function (response) {
                        if (response.redirect) {
                            window.location.href = response.redirect;
                        } else {
                            $('#loginModal .modal-body').html(response);
                            $('#loginModal').modal('show');
                        }
                    });
                }
            });

            // AJAX form submission for registration
            $('#registerForm').on('submit', function (event) {
                event.preventDefault();
                console.log("Form submitted, performing AJAX request...");  // Debugging line
                $.ajax({
                    url: $(this).attr('action'),
                    type: 'POST',
                    data: $(this).serialize(),
                    success: function (response) {
                        console.log("AJAX Success:", response);  // Debugging line
                        if (response.status === "success") {
                            alert(response.message);  // Show pop-up message
                        } else {
                            alert("Registration failed: " + response.message);  // Show pop-up message for failure as well
                        }
                    },
                    error: function (xhr, status, error) {
                        console.log("AJAX Error:", xhr, status, error);  // Debugging line
                        alert('An error occurred: ' + xhr.status + ' ' + xhr.statusText);
                    }
                });
            });

        });

    </script>

    @await RenderSectionAsync("Scripts", required: false)
    <footer class="bg-dark text-white text-center py-3">
        <div class="container">
            <ul class="nav justify-content-center">
                <li class="nav-item"><a class="nav-link text-white" href="@Url.Action("Index", "Home")">Home</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="@Url.Action("About", "Home")">About Us</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="@Url.Action("News", "Home")">Blog/News</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="@Url.Action("Events", "Home")">Events</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="@Url.Action("Resources", "Home")">Resources</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="#">Contact Us</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="javascript:openLoginModal()">Member Login</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="javascript:openRegisterModal()">Register</a></li>
            </ul>
        </div>
        <p>&copy; 2023 Kompisar Investment Club. All rights reserved.</p>
    </footer>

</body>
</html>﻿
